# 密度计数据自动采集系统 - 开发需求文档与计划

## 1. 项目概述 (Project Overview)
本项目旨在开发一款桌面应用程序，用于自动采集电子密度计通过串口传输的测试数据，并结合预设的产品规格标准进行自动判定（合格/不合格），最终将测试结果自动回写至 Excel 报表中。该系统旨在替代人工记录，提高数据录入的准确性和效率。

## 2. 需求规格说明 (Requirements Specification)

### 2.1 功能性需求 (Functional Requirements)

#### 2.1.1 串口通信管理
- **自动检测**: 程序启动时应自动扫描当前可用的 COM 端口。
- **参数配置**: 支持通过 `config.ini` 配置文件设定波特率、数据位、停止位等参数（默认 9600, 8, N, 1）。
- **连接控制**: 提供“打开串口”和“关闭串口”的手动控制功能，并实时显示连接状态。

#### 2.1.2 产品规格管理 (Excel 集成)
- **数据源读取**: 程序需读取 `density_data.xlsx` 文件。
- **关键字段**: 识别“产品型号”、“密度下限”、“密度上限”等关键列。
- **型号选择**: 在 UI 上提供下拉菜单，列出 Excel 中存在的所有产品型号供操作员选择。
- **自动填充**: 选择型号后，自动在界面显示对应的密度上下限标准。

#### 2.1.3 数据采集与解析
- **实时监听**: 串口连接后，后台线程实时监听设备发送的数据。
- **协议解析**:
  - 密度数据格式：支持解析类似 `D=1.234 g/cm3` 的字符串。
  - 温度数据格式：支持解析类似 `T=25.0 C` 的字符串。
- **抗干扰**: 具备一定的容错能力，忽略非标准格式的杂乱数据。

#### 2.1.4 自动判定逻辑
- **合格判断**: 采集到的密度值需在 [下限, 上限] 范围内。
- **结果反馈**:
  - **界面反馈**: 通过醒目的颜色（绿色=合格，红色=不合格）和文字提示判定结果。
  - **弹窗提示**: 完成一次有效测试后，通过弹窗告知用户测试结束及结果。

#### 2.1.5 数据记录与导出
- **信息录入**: 界面需提供输入框，供操作员输入“批号”和“测试员”信息。
- **Excel 回写**:
  - 将测试结果（密度值、温度、判定结果、测试时间、测试员、批号）写入 `density_data.xlsx`。
  - **智能匹配**: 根据选定的“产品型号”查找 Excel 对应行进行更新。
  - **新增追加**: 若 Excel 中不存在该型号（或需保留历史记录），支持在表格末尾追加新行。
- **文件保护**: 写入数据时若文件被占用，应进行异常处理并提示用户关闭文件。

### 2.2 非功能性需求 (Non-Functional Requirements)
- **易用性**: 界面简洁直观，操作流程符合产线作业习惯（选择型号 -> 开始 -> 测量 -> 自动保存）。
- **兼容性**: 支持 Windows 10/11 操作系统。
- **便携性**: 最终交付物需为单文件 `.exe`，无需安装 Python 环境即可运行。
- **稳定性**: 长时间运行不崩溃，串口断开或 Excel 占用时有明确的错误提示而非闪退。

---

## 3. 开发计划 (Development Plan)

### 第一阶段：原型验证与基础框架 (Week 1)
- **目标**: 跑通串口读取和 Excel 读取的核心链路。
- **任务**:
  1. 搭建 Python + Tkinter GUI 基础框架。
  2. 实现 `config.ini` 读取与串口连接功能。
  3. 实现 `openpyxl` 读取 Excel 并在下拉框显示产品型号。
  4. 编写串口模拟脚本，验证数据接收逻辑。

### 第二阶段：核心业务逻辑实现 (Week 1-2)
- **目标**: 实现数据解析、自动判定和界面交互。
- **任务**:
  1. 开发正则表达式模块，准确提取 `D=` 和 `T=` 数值。
  2. 实现界面上的“开始测试/停止测试”状态机逻辑。
  3. 完成密度值与上下限的比较逻辑，并绑定 UI 颜色变化。
  4. 实现基础的 Excel 回写功能（暂不处理复杂异常）。

### 第三阶段：异常处理与功能增强 (Week 2)
- **目标**: 提升软件鲁棒性，处理各种边缘情况。
- **任务**:
  1. **Excel 占用处理**: 增加 `try-except` 块，当 Excel 被打开时提示用户。
  2. **新增行逻辑**: 优化回写逻辑，当找不到型号时自动追加新行。
  3. **线程安全**: 确保串口线程与 GUI 线程的通信安全（使用 `after` 或队列）。
  4. **输入校验**: 强制要求“批号”和“测试员”非空才能保存。

### 第四阶段：打包与交付 (Week 3)
- **目标**: 生成可执行文件并编写文档。
- **任务**:
  1. 使用 `PyInstaller` 配置打包脚本 (`build_exe.ps1`)。
  2. 解决打包后的路径依赖问题（如 `config.ini` 位置）。
  3. 编写 `README.md` 操作手册。
  4. 编写自动化构建脚本 (`build_exe.bat`) 实现一键打包。
  5. 最终集成测试与验收。

## 4. 技术栈 (Technology Stack)
- **编程语言**: Python 3.x
- **GUI 框架**: Tkinter (内置，轻量级)
- **串口通信**: pyserial
- **Excel 处理**: openpyxl
- **打包工具**: PyInstaller
